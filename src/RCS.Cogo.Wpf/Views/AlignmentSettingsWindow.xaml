<Window x:Class="RCS.Cogo.Wpf.Views.AlignmentSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Alignment Settings" Height="360" Width="400"
        Background="{StaticResource BrBg}" Foreground="{StaticResource BrTextPrimary}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Background="{StaticResource BrBg}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ── Horizontal Alignment ──────────────────────────────────── -->
        <TextBlock Grid.Row="0" Text="Horizontal" FontWeight="Bold" FontSize="16"
                   Foreground="{StaticResource BrAccent}" Margin="0,0,0,8"/>
        
        <CheckBox Grid.Row="1" Content="Show Horizontal Alignment Labels"
                  IsChecked="{Binding ShowHorizontalAlignmentLabels}"
                  Margin="0,0,0,4"/>

        <Separator Grid.Row="2" Margin="0,10"/>

        <!-- ── Vertical Alignment ────────────────────────────────────── -->
        <TextBlock Grid.Row="3" Text="Vertical" FontWeight="Bold" FontSize="16"
                   Foreground="{StaticResource BrAccent}" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="4" Orientation="Vertical">
            <CheckBox Content="Show Vertical Alignment Labels"
                      IsChecked="{Binding ShowVerticalAlignmentLabels}"
                      Margin="0,0,0,6"/>
            <CheckBox Content="Show VPIs (Vertical Point of Intersection)"
                      IsChecked="{Binding ShowVPIs}"
                      Margin="0,0,0,6"/>
            <CheckBox Content="Show Grade Percentage on Profile"
                      IsChecked="{Binding ShowGradePercent}"
                      Margin="0,0,0,0"/>
        </StackPanel>

        <!-- ── Footer ────────────────────────────────────────────────── -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Close" Width="80" Height="30"
                    Click="CloseButton_Click"
                    Background="{StaticResource BrBorder}" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
