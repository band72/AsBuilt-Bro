<Window x:Class="RCS.Cogo.Wpf.Views.EditPointsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Points" Height="480" Width="640"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <!-- Text-link button style for the inline description cell button -->
        <Style x:Key="TextButtonStyle" TargetType="Button" BasedOn="{StaticResource BtnGhost}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Foreground"                Value="{StaticResource BrAccent}"/>
            <Setter Property="Height"                    Value="Auto"/>
            <Setter Property="Padding"                   Value="2,0"/>
            <Setter Property="BorderThickness"           Value="0"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource BrBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="52"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource WindowHeaderBand}">
            <TextBlock Text="📍  Edit Project Points"
                       VerticalAlignment="Center"
                       FontSize="16" FontWeight="SemiBold"
                       Foreground="{StaticResource BrTextPrimary}"/>
        </Border>

        <!-- Data Grid -->
        <DataGrid x:Name="PointsGrid" Grid.Row="1"
                  ItemsSource="{Binding Points}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  Margin="10,10,10,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Point #" Binding="{Binding Id}"
                                    Width="90" IsReadOnly="True"/>
                <DataGridTextColumn Header="Northing"  Binding="{Binding Northing, StringFormat=F3}"  Width="110"/>
                <DataGridTextColumn Header="Easting"   Binding="{Binding Easting, StringFormat=F3}"   Width="110"/>
                <DataGridTextColumn Header="Elevation" Binding="{Binding Elevation, StringFormat=F3}" Width="110"/>
                <DataGridTemplateColumn Header="Description (click to edit)"
                                        SortMemberPath="Description" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Description, TargetNullValue='[Empty]'}"
                                    Style="{StaticResource TextButtonStyle}"
                                    Command="{Binding DataContext.EditDescriptionCommand,
                                              RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <Border Grid.Row="2" Style="{StaticResource WindowFooterBand}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Close" IsCancel="True"
                        Style="{StaticResource BtnGhost}" Width="90"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
