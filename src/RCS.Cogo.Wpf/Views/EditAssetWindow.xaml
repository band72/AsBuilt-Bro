<Window x:Class="RCS.Cogo.Wpf.Views.EditAssetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Installed Asset" Height="650" Width="500"
        Background="#1E1E1E" Foreground="#D4D4D4"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Edit Asset Record" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="PartKey:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding PartKey}" IsReadOnly="False" Background="#333" Foreground="White" Padding="5"/>
        </Grid>

        <Border Grid.Row="2" BorderBrush="#3E3E42" BorderThickness="1" CornerRadius="4" Margin="0,20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10">
                    <!-- Dynamic form rows -->
                    <StackPanel>
                        <TextBlock Text="Discipline:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding Discipline}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Feature Type:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding FeatureType}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Size:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding Size}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Material:" Margin="0,5,0,0"/>
                        <ComboBox Text="{Binding Material}" ItemsSource="{Binding MaterialsList, RelativeSource={RelativeSource AncestorType=Window}}" IsEditable="True" Background="White" Foreground="Black" Padding="5"/>
                    </StackPanel>

                    <!-- Core Added JEA Common Fields -->
                    <StackPanel>
                        <TextBlock Text="Subtype:" Margin="0,5,0,0"/>
                        <ComboBox Text="{Binding Subtype}" ItemsSource="{Binding SubtypesList, RelativeSource={RelativeSource AncestorType=Window}}" IsEditable="True" Background="White" Foreground="Black" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Facility Owner:" Margin="0,5,0,0"/>
                        <ComboBox Text="{Binding FacilityOwner}" ItemsSource="{Binding FacilityOwnersList, RelativeSource={RelativeSource AncestorType=Window}}" IsEditable="True" Background="White" Foreground="Black" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Size Secondary:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding SizeSecondary}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Pipe Class:" Margin="0,5,0,0"/>
                        <ComboBox Text="{Binding PipeClass}" ItemsSource="{Binding PipeClassList, RelativeSource={RelativeSource AncestorType=Window}}" IsEditable="True" Background="White" Foreground="Black" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Lining Manufacturer:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding LiningManufacturer}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Lining Material:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding LiningMaterial}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Orientation:" Margin="0,5,0,0"/>
                        <ComboBox Text="{Binding Orientation}" ItemsSource="{Binding OrientationsList, RelativeSource={RelativeSource AncestorType=Window}}" IsEditable="True" Background="White" Foreground="Black" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Pipe Role:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding PipeRole}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Drop Type:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding DropType}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Invert Elevations w/ Directions:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding InvertElevationsWithDirections}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>
                    
                    <StackPanel>
                        <TextBlock Text="Exterior Joint Tape Type:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding ExteriorJointTapeType}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>
                    
                    <StackPanel>
                        <TextBlock Text="Exterior Joint Tape Mfr:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding ExteriorJointTapeManufacturer}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <!-- Point Geometry (Structures, Valves, Meters, etc) -->
                    <Grid Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Text="Northing:"/>
                            <TextBox Text="{Binding Northing, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0,5,0">
                            <TextBlock Text="Easting:"/>
                            <TextBox Text="{Binding Easting, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="5,0,0,0">
                            <TextBlock Text="Top/Grade Elev:"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Text="{Binding TopElevation, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5" Margin="0,0,2,0"/>
                                <TextBox Grid.Column="1" Text="{Binding GradeElevation, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5" Margin="2,0,0,0"/>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <!-- Line Geometry (Pipes) -->
                    <Grid Margin="0,15,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Text="Upstream Invert:"/>
                            <TextBox Text="{Binding UpstreamInvert, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0,5,0">
                            <TextBlock Text="Upstream Grade:"/>
                            <TextBox Text="{Binding UpstreamGrade, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="5,0,0,0">
                            <TextBlock Text="Length / Slope:"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Text="{Binding Length, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5" Margin="0,0,2,0"/>
                                <TextBox Grid.Column="1" Text="{Binding Slope, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5" Margin="2,0,0,0"/>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Text="Downstream Invert:"/>
                            <TextBox Text="{Binding DownstreamInvert, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0,5,0">
                            <TextBlock Text="Downstream Grade:"/>
                            <TextBox Text="{Binding DownstreamGrade, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="5,0,0,0">
                        </StackPanel>
                    </Grid>

                    <StackPanel Margin="0,5,0,0">
                        <TextBlock Text="Quantity:"/>
                        <TextBox Text="{Binding Quantity}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <!-- Manufacturer info -->
                    <StackPanel Margin="0,15,0,0">
                        <TextBlock Text="Manufacturer:" FontWeight="Bold" Foreground="CornflowerBlue"/>
                        <TextBox Text="{Binding Manufacturer}" Background="#252526" Foreground="White" Padding="5" Margin="0,5,0,0"/>
                    </StackPanel>

                    <StackPanel Margin="0,5,0,0">
                        <TextBlock Text="Model / Part No:"/>
                        <TextBox Text="{Binding ManufacturerPartNo}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <StackPanel Margin="0,5,0,0">
                        <TextBlock Text="Year Manufactured:"/>
                        <TextBox Text="{Binding YearManufactured}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <TextBlock x:Name="MsgTxt" Grid.Row="3" Foreground="LightGreen" Margin="0,0,0,10"/>
        
        <Grid Grid.Row="4">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Materials" Click="Materials_Click" Padding="10,5" Margin="0,0,10,0" Background="#2D2D30" Foreground="White"/>
                <Button Content="Universal" Click="Universal_Click" Padding="10,5" Margin="0,0,10,0" Background="#2D2D30" ToolTip="Apply these metadata settings to all other records in the current grid" Foreground="White"/>
                <Button Content="Delete" Click="Delete_Click" Padding="10,5" Margin="0,0,10,0" Background="#2D2D30" Foreground="White"/>
                <Button Content="Cancel" Click="Cancel_Click" Padding="10,5" Margin="0,0,10,0" Background="#2D2D30" Foreground="White"/>
                <Button Content="Save" Click="Save_Click" Padding="10,5" Background="#2D2D30" Foreground="White"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
