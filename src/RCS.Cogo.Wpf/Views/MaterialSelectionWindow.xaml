<Window x:Class="RCS.Cogo.Wpf.Views.MaterialSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Material" Height="450" Width="800"
        Background="#1E1E1E" Foreground="#D4D4D4"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Schedule of Materials" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="SearchBox" Width="200" Background="#333" Foreground="White" Padding="5" TextChanged="SearchBox_TextChanged"/>
        </StackPanel>

        <DataGrid x:Name="MaterialsGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                  Background="#252526" RowBackground="#252526" Foreground="#D4D4D4" BorderThickness="1" BorderBrush="#3E3E42"
                  MouseDoubleClick="MaterialsGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Part Key" Binding="{Binding PartKey}" Width="*"/>
                <DataGridTextColumn Header="Discipline" Binding="{Binding Discipline}" Width="100"/>
                <DataGridTextColumn Header="Feature Type" Binding="{Binding FeatureType}" Width="100"/>
                <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="80"/>
                <DataGridTextColumn Header="Material" Binding="{Binding Material}" Width="80"/>
                <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}" Width="120"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancel" Click="Cancel_Click" Padding="15,5" Margin="0,0,10,0" Background="#2D2D30" Foreground="White"/>
            <Button Content="Select" Click="Select_Click" Padding="25,5" Background="#007ACC" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
