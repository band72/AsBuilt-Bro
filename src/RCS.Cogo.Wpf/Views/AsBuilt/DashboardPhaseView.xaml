<UserControl x:Class="RCS.Cogo.Wpf.Views.AsBuilt.DashboardPhaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" Background="Transparent"
             d:DesignHeight="700" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Enterprise Analytics" Foreground="#CDD6F4" FontSize="32" FontWeight="Bold" FontFamily="Segoe UI" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" Text="Pipeline Integrity Overview" Foreground="#BAC2DE" FontSize="16" FontFamily="Segoe UI" Margin="0,0,0,32"/>
        
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Background="#1E1E2E" CornerRadius="8" Padding="24" Margin="0,0,12,0" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Total Points" Foreground="#BAC2DE" FontSize="14" FontFamily="Segoe UI"/>
                    <TextBlock Text="{Binding Job.Points.Count}" Foreground="#89B4FA" FontSize="48" FontWeight="Black" FontFamily="Segoe UI" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
            
            <Border Grid.Column="1" Background="#1E1E2E" CornerRadius="8" Padding="24" Margin="12,0" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Structures" Foreground="#BAC2DE" FontSize="14" FontFamily="Segoe UI"/>
                    <TextBlock Text="{Binding Job.Network.Structures.Count}" Foreground="#A6E3A1" FontSize="48" FontWeight="Black" FontFamily="Segoe UI" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
            
            <Border Grid.Column="2" Background="#1E1E2E" CornerRadius="8" Padding="24" Margin="12,0,0,0" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Line Matrix" Foreground="#BAC2DE" FontSize="14" FontFamily="Segoe UI"/>
                    <TextBlock Text="{Binding Job.Network.Runs.Count}" Foreground="#F38BA8" FontSize="48" FontWeight="Black" FontFamily="Segoe UI" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
