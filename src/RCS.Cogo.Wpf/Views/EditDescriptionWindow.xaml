<Window x:Class="RCS.Cogo.Wpf.Views.EditDescriptionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Description" Height="200" Width="300"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1E1E1E" Foreground="#D4D4D4">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#D4D4D4"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Current Description:" Margin="0,0,0,5"/>
        <TextBox x:Name="DescriptionTextBox" Grid.Row="1" Text="{Binding SelectedDescription, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Select Local Code:" Margin="0,0,0,5"/>
        <ComboBox x:Name="LocalCodesComboBox" Grid.Row="3" VerticalAlignment="Top"
                  ItemsSource="{Binding AvailableCodes}" 
                  SelectedItem="{Binding SelectedDescription}" 
                  IsEditable="True" 
                  StaysOpenOnEdit="True"
                  Background="Black" Foreground="White" BorderBrush="#3E3E42"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Update" IsDefault="True" Click="Update_Click" Width="80" Margin="0,0,10,0" Background="#007ACC" Foreground="White" BorderThickness="0" Padding="5"/>
            <Button Content="Cancel" IsCancel="True" Width="80" Background="#3E3E42" Foreground="White" BorderThickness="0" Padding="5"/>
        </StackPanel>
    </Grid>
</Window>
