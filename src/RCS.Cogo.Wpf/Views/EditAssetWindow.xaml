<Window x:Class="RCS.Cogo.Wpf.Views.EditAssetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Installed Asset" Height="650" Width="500"
        Background="#1E1E1E" Foreground="#D4D4D4"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Edit Asset Record" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="PartKey:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding PartKey}" IsReadOnly="False" Background="#333" Foreground="White" Padding="5"/>
        </Grid>

        <Border Grid.Row="2" BorderBrush="#3E3E42" BorderThickness="1" CornerRadius="4" Margin="0,20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10">
                    <!-- Dynamic form rows -->
                    <StackPanel>
                        <TextBlock Text="Discipline:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding Discipline}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Feature Type:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding FeatureType}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Size:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding Size}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Material:" Margin="0,5,0,0"/>
                        <TextBox Text="{Binding Material}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <!-- Point Geometry (Structures, Valves, Meters, etc) -->
                    <Grid Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Text="Northing:"/>
                            <TextBox Text="{Binding Northing, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0,5,0">
                            <TextBlock Text="Easting:"/>
                            <TextBox Text="{Binding Easting, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="5,0,0,0">
                            <TextBlock Text="Elevation/Invert:"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding Elevation, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                                <Button Grid.Column="1" Content="..." Width="25" Margin="5,0,0,0" Tag="Elevation" Click="CalculateInvert_Click"/>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <!-- Line Geometry (Pipes) -->
                    <Grid Margin="0,15,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Text="Start Northing:"/>
                            <TextBox Text="{Binding NorthingStart, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0,5,0">
                            <TextBlock Text="Start Easting:"/>
                            <TextBox Text="{Binding EastingStart, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="5,0,0,0">
                            <TextBlock Text="Start Invert:"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding InvertStart, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                                <Button Grid.Column="1" Content="..." Width="25" Margin="5,0,0,0" Tag="InvertStart" Click="CalculateInvert_Click"/>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Text="End Northing:"/>
                            <TextBox Text="{Binding NorthingEnd, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0,5,0">
                            <TextBlock Text="End Easting:"/>
                            <TextBox Text="{Binding EastingEnd, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="5,0,0,0">
                            <TextBlock Text="End Invert:"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding InvertEnd, StringFormat=\{0:F3\}}" Background="#252526" Foreground="White" Padding="5"/>
                                <Button Grid.Column="1" Content="..." Width="25" Margin="5,0,0,0" Tag="InvertEnd" Click="CalculateInvert_Click"/>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <StackPanel Margin="0,5,0,0">
                        <TextBlock Text="Quantity:"/>
                        <TextBox Text="{Binding Quantity}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <!-- Manufacturer info -->
                    <StackPanel Margin="0,15,0,0">
                        <TextBlock Text="Manufacturer:" FontWeight="Bold" Foreground="CornflowerBlue"/>
                        <TextBox Text="{Binding Manufacturer}" Background="#252526" Foreground="White" Padding="5" Margin="0,5,0,0"/>
                    </StackPanel>

                    <StackPanel Margin="0,5,0,0">
                        <TextBlock Text="Model / Part No:"/>
                        <TextBox Text="{Binding ManufacturerPartNo}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <StackPanel Margin="0,5,0,0">
                        <TextBlock Text="Year Manufactured:"/>
                        <TextBox Text="{Binding YearManufactured}" Background="#252526" Foreground="White" Padding="5"/>
                    </StackPanel>

                    <StackPanel Margin="0,15,0,0">
                        <TextBlock Text="Confidence/Source:" FontWeight="Bold" Foreground="CornflowerBlue"/>
                        <Grid Margin="0,5,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding Confidence}" Background="#252526" Foreground="White" Padding="5" Margin="0,0,5,0"/>
                            <TextBox Grid.Column="1" Text="{Binding Source}" Background="#252526" Foreground="White" Padding="5" Margin="5,0,0,0"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Margin="0,5,0,0">
                        <TextBlock Text="Description (User):"/>
                        <TextBox Text="{Binding Description}" Background="#252526" Foreground="White" Padding="5" AcceptsReturn="False"/>
                    </StackPanel>
                    
                    <StackPanel Margin="0,5,0,0">
                        <TextBlock Text="Notes:"/>
                        <TextBox Text="{Binding Notes}" IsReadOnly="True" Background="#252526" Foreground="White" Padding="5" AcceptsReturn="True" Height="60" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <TextBlock x:Name="MsgTxt" Grid.Row="3" Foreground="LightGreen" Margin="0,0,0,10"/>
        
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" Content="Materials" Click="Materials_Click" Padding="15,5" Background="#6B8E23" Foreground="White" FontWeight="Bold"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Delete Asset" Click="Delete_Click" Padding="15,5" Margin="0,0,10,0" Background="#A52A2A" Foreground="White" FontWeight="Bold"/>
                <Button Content="Cancel" Click="Cancel_Click" Padding="15,5" Margin="0,0,10,0" Background="#2D2D30" Foreground="White"/>
                <Button Content="Save to DB" Click="Save_Click" Padding="25,5" Background="#007ACC" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
